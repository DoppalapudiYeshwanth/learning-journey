<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Whatsapp</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>All Chats</h1>
    <form method="GET" action="/chats/new">
        <button class="new-button">new chat</button>
    </form>
    <% for(chat of chats){%>
        <div class="msg">
        <p>To : <%=chat.to%></p>
        <p>Message : <%=chat.message%></p>
        <p>Created At :<%=chat.created_at%></p>
        <p>Send by : <%=chat.from%></p>
        <p>Created Time :<%=chat.created_at.toString().split(" ")[4]%></p>
        <p>Created Date : <%=chat.created_at.toString().split(" ").slice(0,4).join(" ")%></p>
        <p>Updated Time :<%=chat.updated_at.toString().split(" ")[4]%></p>
        <p>Updated Date : <%=chat.updated_at.toString().split(" ").slice(0,4).join(" ")%></p>
        <form method="GET" action="/chats/<%=chat._id%>/edit">
            <button>Edit the post</button>
        </form>
        <form method="POST" action="/chats/<%=chat._id%>/delete?_method=DELETE">
            <button>Delete</button>
        </form>
        <hr>
        </div>
    <%}%>
</body>
</html>